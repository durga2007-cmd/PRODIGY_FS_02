import { Department, Employee, EmployeeStatus } from "./types";

// Empty array for a fresh application start
export const MOCK_EMPLOYEES: Employee[] = [];

// Updated keys to ensure a clean local storage session
export const AUTH_TOKEN_KEY = 'hr_sys_auth_token_new';
export const STORAGE_KEY = 'hr_sys_data_new';